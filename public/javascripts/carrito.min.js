function precioProducto(id, precio) {
    var cantidad = $('#cantidad' + id).val();
    var total = precio * cantidad;
    //console.log(total);
    $('#totalproducto' + id).text(total);
    //console.log(cantidad);
}